{
  "private": true,
  "scripts": {
    "build": "npm --prefix packages/excalidrop run build",
    "dev": "npm --prefix packages/excalidrop run dev",
    "type-check": "npm --prefix packages/excalidrop run type-check"
  }
}
